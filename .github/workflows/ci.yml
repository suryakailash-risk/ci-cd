name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # - name: Install Dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -r requirements.txt

      # Run the app.py script
      - name: Logging to Linux Maching
        run: |
          ssh -t root@62.72.27.131 'Yes'
      - name: Entering the pass for tempory
        run: |
          v6mcSijHt#Ox8E3nrY=Z
      - name: Listing inside VM
        run: |
          ls
